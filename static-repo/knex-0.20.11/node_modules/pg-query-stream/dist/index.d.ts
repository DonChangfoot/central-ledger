declare var Cursor: any;
declare var Readable: any;
declare class PgQueryStream extends Readable {
    constructor(text: any, values: any, options: any);
    submit(connection: any): void;
    close(callback: any): void;
    _read(size: any): boolean;
}
